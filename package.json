{
  "name": "learning-genie-sync",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "dependencies": {
    "commander": "^12.1.0",
    "date-fns": "^4.1.0",
    "playwright": "^1.55.1"
  },
  "scripts": {
    "sync":  "node ./lg.mjs sync",
    "docker-build": "docker build -t learning-genie-sync ."
  }
}
